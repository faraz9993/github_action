name: checkout
on: [push]

jobs:
    checkout-action:
        runs-on: ubuntu-latest
        steps:
            - name: List file before
              run: ls -la
            - uses: action/checkout@v3
            - name: List files after
              run: ls -la
